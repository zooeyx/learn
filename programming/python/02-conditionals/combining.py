"""Combining Conditions — nested conditions and compound expressions.

Exercises:
  1. Write nested if statements for a multi-step decision
  2. Use compound conditions with and/or
  3. Implement a simple eligibility checker (age + income)
  4. Use chained comparisons (e.g., 1 < x < 10)
"""


def main():
    # TODO: Nested if — check if a number is positive, then even/odd
    # Hint: if num > 0:
    #           if num % 2 == 0: ...

    # TODO: Compound condition — check if age is between 18 and 65
    # Hint: if 18 <= age <= 65:  (Python supports chained comparisons)

    # TODO: Eligibility checker — must be 18+ AND income >= 30000
    # Hint: if age >= 18 and income >= 30000:

    # TODO: Demonstrate chained comparisons
    # Hint: x = 5; print(f"1 < {x} < 10: {1 < x < 10}")

    pass  # Remove when done


if __name__ == "__main__":
    main()
