"""Loop Patterns — common patterns using loops.

Exercises:
  1. Accumulator: sum numbers 1-100
  2. Search: find first negative in a list
  3. Filter: collect even numbers from a list
  4. Max/min: find largest without max()
"""


def main():
    # TODO: Accumulator — sum of 1 to 100
    # Hint: total = 0; for i in range(1, 101): total += i

    # TODO: Search — find first negative number
    # Hint: numbers = [4, 7, -2, 8, -5, 3]

    # TODO: Filter — collect all even numbers into a new list
    # Hint: evens = []; for n in numbers: if n % 2 == 0: evens.append(n)

    # TODO: Max — find the largest number without using max()
    # Hint: largest = numbers[0]; for n in numbers: ...

    pass  # Remove when done


if __name__ == "__main__":
    main()
