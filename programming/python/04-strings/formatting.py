"""Formatting — f-strings, format specification, and alignment.

Exercises:
  1. Basic f-string expressions
  2. Number formatting: decimals, thousands, percentage
  3. String alignment: left, right, center with fill
  4. Format a simple table with aligned columns
"""


def main():
    # TODO: Basic f-string with expressions
    # Hint: x = 10; print(f"{x} squared is {x**2}")

    # TODO: Number formatting
    # Hint: pi = 3.14159; print(f"Pi: {pi:.2f}")
    # Hint: big = 1000000; print(f"Big: {big:,}")
    # Hint: ratio = 0.756; print(f"Ratio: {ratio:.1%}")

    # TODO: String alignment (width=20)
    # Hint: print(f"{'left':<20}")
    # Hint: print(f"{'right':>20}")
    # Hint: print(f"{'center':^20}")
    # Hint: print(f"{'padded':*^20}")

    # TODO: Format a table of (name, score) pairs
    # Hint: data = [("Alice", 95), ("Bob", 87), ("Charlie", 92)]
    # Hint: print(f"{'Name':<10} {'Score':>5}")
    # Hint: for name, score in data: print(f"{name:<10} {score:>5}")

    pass  # Remove when done


if __name__ == "__main__":
    main()
