"""String Methods — common string manipulation methods.

Exercises:
  1. Use case methods: upper, lower, title, capitalize, swapcase
  2. Use search methods: find, count, startswith, endswith
  3. Use modification methods: strip, replace, split, join
  4. Use testing methods: isdigit, isalpha, isalnum, isspace
"""


def main():
    s = "  Hello, World!  "

    # TODO: Print results of case methods on s.strip()
    # Hint: print(f"upper: {s.strip().upper()}")

    # TODO: Use find() and count() on a sentence
    # Hint: sentence = "the cat sat on the mat"
    #        print(f"find 'cat': {sentence.find('cat')}")

    # TODO: Use strip(), replace(), split()
    # Hint: print(f"stripped: {s.strip()!r}")
    # Hint: words = "one,two,three".split(",")

    # TODO: Use join() to combine a list of words
    # Hint: print(" ".join(["Hello", "World"]))

    # TODO: Test strings with isdigit(), isalpha(), etc.
    # Hint: print(f"'123'.isdigit(): {'123'.isdigit()}")

    pass  # Remove when done


if __name__ == "__main__":
    main()
