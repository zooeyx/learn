"""Sets — creation, operations, and use cases.

Exercises:
  1. Create sets, add/remove elements
  2. Set operations: union, intersection, difference, symmetric diff
  3. Use sets to remove duplicates
  4. Use sets for fast membership testing
"""


def main():
    # TODO: Create a set, add elements, try adding duplicates
    # Hint: s = {1, 2, 3}; s.add(4); s.add(2)

    # TODO: Set operations
    # Hint: a = {1,2,3,4}; b = {3,4,5,6}
    # Union: a | b
    # Intersection: a & b
    # Difference: a - b
    # Symmetric diff: a ^ b

    # TODO: Remove duplicates from a list using set
    # Hint: unique = list(set([1,2,2,3,3,3]))

    # TODO: Fast membership test — compare list vs set lookup
    # Hint: big_list = list(range(10000))
    # Hint: big_set = set(big_list)
    # Hint: 9999 in big_set  # O(1) vs O(n) for list

    pass  # Remove when done


if __name__ == "__main__":
    main()
