# 11 — Projects

Multi-file builds. Each subdirectory has its own Makefile.

## Projects

- [ ] `calculator/` — Expression parser + evaluator
- [ ] `shell/` — Simple Unix shell (fork, exec, pipes)
- [ ] `http/` — HTTP server from scratch (8 lessons)
