#ifndef HTTP_H
#define HTTP_H

#include <stddef.h>

// TODO: Declare HTTP functions
//   void http_handle_request(int client_fd, const char *docroot);
//   void http_send_response(int fd, int status, const char *status_text,
//                           const char *content_type, const char *body, size_t body_len);
//   void http_send_error(int fd, int status, const char *text);
//   const char *http_get_mime_type(const char *path);

#endif
